{
  "name": "tankstruct-screening",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.cjs",
    "serve": "python -m http.server 4174",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser.cjs",
    "validate": "npm run build && npm test && npm run test:browser"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  },
  "dependencies": {
    "jspdf": "^4.2.1"
  }
}
